Q1 : Pourquoi <Navigate /> (composant) et pas navigate() (hook) ici ? 
 <Navigate /> c'est du JSX  ça va dans un return
navigate() c'est une fonction  ça va dans un handler ou un effet.
 Q2 : Quelle différence entre navigate(from) et navigate(from, { replace: true }) ? 
 navigate(from):il permet ajouter les entrer a l'historique.
 navigate(from, { replace: true }): on permet de pas revenir en  arrière.
 Q3 : Après un POST, pourquoi fait-on setProjects(prev => [...prev, data]) plutôt qu’un 
re-fetch GET ?
re-fetch GET: il permet de recuperer les donnes que tu as deja 
setProjects(prev => [...prev, data]): en serveur il repond au post avec le projet cree on ajoute directement en state 
Q5 : Quelle différence entre <Link> et <NavLink> ? Pourquoi NavLink ici ? 
link: pour les liens.
navlink: pour les navigator.
Q6: les deuc composant ne change pas mais dans put le formumaire il pré-remplis et post le formumaire est vide 
Q7: Oui le message d'erreur s'affiche.
Q8: Avec Axios un 404 lance une erreur automatiquement donc le catch est déclenché et affiche "Erreur 404" Avec fetch il n'y aurait rien

tp4 
| Critère                       | Material UI (MUI)                                        | React-Bootstrap                       |
| ----------------------------- | -------------------------------------------------------- | ------------------------------------- |
| Installation                  | npm install @mui/material @emotion/react @emotion/styled | npm install react-bootstrap bootstrap |
| Nombre de composants utilisés | Élevé (beaucoup de composants riches)                    | Moyen (composants basiques)           |
| Lignes de CSS écrites         | Très peu (style intégré)                                 | Moyen (souvent besoin de custom CSS)  |
| Système de style              | CSS-in-JS (sx, styled)                                   | Classes Bootstrap (className)         |
| Personnalisation couleurs     | Très facile (theme global)                               | Limitée ou nécessite override CSS     |
| Responsive                    | Très bon (Grid, breakpoints)                             | Bon (Grid Bootstrap)                  |
| Lisibilité du code            | Propre mais parfois complexe                             | Simple et facile à lire               |
| Documentation                 | Très complète et moderne                                 | Bonne mais moins détaillée            |

Question 4 , jai choisiez le Material UI
React (localhost:5173) --> Axios --> json-server (localhost:4000)  -->db.json (fichier JSON)

avec firebase on est pas besoin de backend 

➤ Q5

React ne peut pas se connecter directement à MySQL parce que c’est une base de données et non une API. En plus, ce serait dangereux au niveau sécurité (exposition des identifiants). Il faut donc passer par un backend.

➤ Q6

On n’utilise pas json-server en production parce qu’il n’est pas sécurisé, il ne supporte pas beaucoup d’utilisateurs et il ne permet pas de gérer une vraie logique métier.

➤ Q7

Firebase permet une connexion directe avec React parce qu’il fournit un système complet (API + sécurité + authentification). Contrairement à MySQL, il est conçu pour être utilisé côté frontend.

➤ Q8

Pour passer en production, il faut remplacer json-server par une vraie base de données, ajouter l’authentification, sécuriser l’application et la déployer en ligne.

➤ Q9

Le risque d’utiliser des libraries comme MUI ou Bootstrap, c’est d’alourdir l’application et de dépendre des mises à jour, ce qui peut parfois casser le code.

➤ Q10

Pour une application de chat en temps réel, je choisis Firebase parce qu’il gère le temps réel facilement sans besoin de backend, contrairement à json-server.